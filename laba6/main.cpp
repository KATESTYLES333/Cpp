#include <iostream>
/*Задана квадратная матрица А размером N*N(N<=10),состоящая из
действительных чисел.Найти произведение наименьших элементов каждого
столбца матрицы
*/
using namespace std;
int matr(int*array,int column,int raw)

{
    for (int i=0;i<=column;++i);
    int temp=0;
   {
    for (int j=0;j<=raw;++j);
    {
        if(array [0][10]<min)
    }
}
min=array[0][i];
temp=temp*min
}
int main()
{
    cout <<  << endl;
    return 0;
}
